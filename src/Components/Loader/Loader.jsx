import React from 'react';
import './Loader.css'

export const Loader = () => {
    return (
        <div className="loader">
            <span className="loader-spiner"></span>
            <h1>Loading...</h1>
        </div>
    );
};